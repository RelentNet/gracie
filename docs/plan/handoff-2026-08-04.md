# 🤝 Orchestrator Handoff — 2026-08-04

> Historical handoff written at the end of the 2026-08-04 session (large build run). Durable detail lives in auto-memory (`alpha-batch-2026-07-29-redeploy-ready.md`); this is the point-in-time snapshot + the open task list.

You are the **ORCHESTRATOR**. You plan, write self-contained briefs, and **dispatch background worktree agents** to do the coding — you do **not** write feature code yourself (keep your context low). You review each agent's PR (build + secret-scan + code read), and **merge on the operator's go/confidence**. Read auto-memory before anything: `MEMORY.md` → **`alpha-batch-2026-07-29-redeploy-ready.md`** (read the "➡️ 2026-08-04 CONTINUATION" line — it lists every PR through #95) → `operating-model.md` → `operability-built-to-outlive-us.md` → `access-model-all-see-all.md` → `follow-ups.md` → `deploy-topology.md` → `dev-db-ddl-access.md`.

## Where we are
Alpha. `main` @ `91e293e`, tree clean. A huge batch is merged. The operator redeployed once mid-session (~before #94), so **#94 (re-dispatch open-to-all) + #95 (theme/dark-mode)** — and anything after that redeploy — **await the NEXT redeploy**. Migrations `0013`/`0014`/`0015` are all applied → **redeploy needs no DB step**.

**⚠️ Live behavior now on (or landing next redeploy):** #87 — bots join **every** meeting with a join link (not just client-linked). Unlinked meetings capture the transcript + hold docs ("Recorded — link a client…"); linking a client auto-generates.

## Operator's open tasks (theirs, not the orchestrator's — remind/verify)
1. **Redeploy** web+worker in Coolify (manual, `~/.coolify_token`, no git webhook) → brings the theme + re-dispatch + all recent work live.
2. **Recall `recording.done` cutover** — subscribe it in the Recall dashboard → flip `bot_config.transcriptProvider` back to `recallai` → verify one meeting. *This is what makes transcription actually complete* (the self-heal requests transcription; the result returns via this webhook).
3. **Rotate `RECALL_WEBHOOK_SECRET`.**
4. **Publish the v0.4.0 GitHub Release** (benefit-framed notes like prior releases; re-draft as needed).

## Open build follow-ups / debt
(Details in `follow-ups.md` + the alpha-batch memory.)
- Dark-logo-variant slot — navy uploaded logos go faint on the dark theme.
- `cancelled` pipeline_status is overloaded for no-client/held meetings → a proper "held"/"skipped" status someday.
- client-overview "Unknown" — a separate `getUserName` mock (small swap).
- calendar-lib date helpers (`app/(app)/calendar/lib/`) aren't in the web test glob → verified via throwaway script only.
- `.in()`-414-at-scale — reuse `apps/web/lib/data/chunked.ts` `selectByIdsChunked` on the calendar backfill, pipeline fleet, documents `.in()` sites as volume grows.
- docx preview regex sanitizer → `sanitize-html` before beta.
- Pipeline §3.3 bulk re-sync + empty-transcript graceful-skip — deferred.
- 3-row master-record YAML-dump backfill — bounded, optional.
- **Verify whether `documents-mark-reviewed` ever shipped** — if the `needs_review` flag still can't be cleared, the brief is `docs/plan/documents-mark-reviewed.md`.

## Remaining roadmap
GS (Gracie SMS) → P10 (Testing/Security/Launch). GF + n8n = v2.

## Rules that bite
- ⭐ Every feature operable by non-technical staff — no console/script/SQL recovery.
- **All-staff-see-all-clients is intended — never build a per-client ACL** (`access-model-all-see-all`).
- Alpha = **no data erasure** — additive migrations only; verify row-counts before/after; apply via `/pg/query` (`dev-db-ddl-access`), coordinated with the operator.
- Roadmap edits via the `roadmap` skill in a worktree — never hand-edit `docs/roadmap.html`.
- `calendar_bot_dispatch_enabled` is ON (bots live).
- **origin/main is the truth** — the local `main` working tree tends to go stale (an uncommitted `docs/roadmap.html` was stashed this session and blocks fast-forwards); agents branch fresh worktrees.
- Recall: ad-hoc bots cap at ~30 concurrent (we dispatch ad-hoc @ 5-min lead); scheduled bots have no cap — the upgrade path if >30 concurrent is ever needed.

## Agent gotcha
The in-app preview tool anchors to the MAIN checkout, so worktree agents must run their **own** `next dev`; they reach the LAN backend by copying the gitignored dev `.env.local` into the worktree.

## Start by
Read memory → `gh pr list` (only stale `#8` should be open — a July docs brief, ignorable/closeable) → confirm `origin/main` @ `91e293e` → ask the operator what to drive next (redeploy + ops tasks, or the next feature/phase).
